retrieval:
  evaluate: True
  path: ${data.QS.path}/gt_corresps.pkl
  k: 10
  similarity: main.histogram_intersection

masking:
  evaluate: False
  path: ${data.QS.path}

text:
  evaluate: False
  path: ${data.QS.path}/text_boxes.pkl

ocr:
  evaluate: True
  path: ${data.QS.path}


path: ${hydra:runtime.cwd}/results/${data.QN}