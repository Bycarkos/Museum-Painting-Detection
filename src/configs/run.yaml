defaults:
  - _self_
  - descriptors: [default]
  - preprocessing: [background_removal, text_removal]
  - evaluation: [default]

data:

  BBDD:
    path: ${hydra:runtime.cwd}/data/BBDD
    importation:

      descriptors:
        import_: True
        path: ${hydra:runtime.cwd}/data/descritptors/pyramidal3d_rg.pkl


    export:

      descriptors:
        save: False
        path: ${hydra:runtime.cwd}/data/descritptors/texture_dct.pkl

  QN: qst1_w4

  QS:
    path: ${hydra:runtime.cwd}/data/${data.QN}

    preprocessed:
      import_: False

    #importation:

    #   preprocessing:
    #    import_: False
    #    path: ${data.QS.path}/${data.QN}_processed.pkl

    #  descriptors:
    #    import_: False
    #    path: ${hydra:runtime.cwd}/data/descritptors

    #export:

    #  descriptors:
    #    export_: True
    #    path: ${hydra:runtime.cwd}/data/descritptors

    #  preprocessing:
    #    export_: True
    #    path: ${data.QS.path}/${data.QN}_processed.pkl